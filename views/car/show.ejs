<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+HK:wght@500&display=swap" rel="stylesheet">
    <script
    src="https://code.jquery.com/jquery-3.5.1.min.js"
    integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
    crossorigin="anonymous" defer></script>
    <script src="/scripts/app.js" defer></script>
 

    <title>Car Details Page</title>
</head>
<body>
    <header>
        <nav>
            <a href="/cars/1">
           <img src="/images/logo2.png" alt="car logo" class="image"> 
         </a>
           <div class="right-menu">
            <% if (user.currentUser) { %>
             
              <a href="/logout">Logout</a> 
            
          </nav>
        </header>
             
            <% }  else { %>
              
                   
                       <a href="/login">Login</a>
                       <a href="/create">Register</a>
                       
            </div> 
                </nav>
                </header>
            <% }%>
           
  <div class="show">
       
        <div class="thumbnail">
        <img src="<%= car.img[0] %> " height="500px" width="650px" id="first">
        
            <div class="smallthumb">
                
                <% if(car.img.length !== 1) {%> 
            <% for( let index = 0; index < car.img.length; index++ ) { %>
                <img src="<%=car.img[index]%>" height="100px" width="100px" class="small">
                <% } %>
                <% } %>
            </div>
            </div>
            <ul>
                <section class="list">
            <p ><li><%= car.year %> <%= car.name %></li></p>
            <p><li>$<%= car.price %> </li></p>
            <p><li><%= car.mileage %> Miles </li></p>
            <p><li>Description: <%= car.description %> </li></p>        
            
            <p><li><a href="/profile/<%= car.user %>">More listings from this user </a></li></p>
        
       
            
       
       
        <% if (user.currentUser) { %>
        <a href="/massage/<%=user.currentUser.username%>/<%=car.user%>" class="msg">Send Message</a>
        <% } %>
        <% if(!user.currentUser) { %>
            Sign up to contact seller
        <% } %> 
    </ul>


</div>
    </section>
    
       
    </section>
    
</body>
</html>