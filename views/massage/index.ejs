<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link rel="stylesheet" href="/styles/main.css">
    <link rel="stylesheet" href="/styles/chat.css">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+HK:wght@500&display=swap" rel="stylesheet">
    <script defer
  src="https://code.jquery.com/jquery-3.5.1.min.js"
  integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
  crossorigin="anonymous"></script>
  <script defer src="/scripts/chat.js"></script>
    <title>Document</title>
</head>
<body>
    <header>
        <nav>
            <a href="/cars/1">
           <img src="/images/logo2.png" alt="car logo" class="image"> 
            </a>
           <div class="right-menu">
            <% if (user.currentUser) { %>
              <a href="/profile/<%=user.currentUser.id%>" class="profile">Profile</a>          
              <a href="/logout">Logout</a> 
             </form>
             
        </nav>
    </header>
             <% }  else { %>
                <a href="/login">Login</a>
                <a href="/create">Register</a>
                       
            </div> 
        </nav>
    </header>
            <% }%>

<div class="chat" id='chat'>
    <h3>Massages</h3>
    <%if (story.length>1) {%>
        <%for(let i=1; i<story.length; i+=3){%>
            <div class="<%=story[i+2]%>" >
                <p class="<%=story[i+2]%>name"><%=story[i+1]%> : </br> <%=story[i]%></p></div>
        <% } %>
            
    <%}%>
</div>
         
            
<div class="chatrooms" >
  

<div class="buyerchat <%if(user.currentUser.username!=buyer){%>
    <%='hidden' %> <%}%>
    "  >
    <form action="/massage/send/buyer" method="POST" id="buyer">  
        <p class="buyerlabel"> Seller</p><input type="text" readonly name="seller" value="<%=seller%>">
        <p></p>
        <p class="buyerlabel"> You</p> <input type="text" readonly name="buyer" value="<%=buyer%>" >  
    <textarea id="buyertext" class='text' name="text"  ></textarea>
    <button type="submit"class="submit" id="buyersubmit" hidden>Send Text</button>
    </form>
    </div>
  <div class="sellerchat"<%if(user.currentUser.username==buyer){%>
    <%='hidden' %> <%}%>
    "  >  
        <form action="/massage/send/seller" method="POST">  
            <p class="sellerlabel"> You</p><input type="text" readonly name="seller" value="<%=seller%>"  >
            <p></p>
            <p class="sellerlabel"> Buyer</p> <input type="text" readonly name="buyer" value="<%=buyer%>"  >  
        <textarea id="sellertext" class='text' name="text" id="" cols="20" rows="1"></textarea>
        <button type="submit"class="submit" id="sellersubmit" hidden>Send Text</button>
        </form>
    </div>
</div>   


</body>
<script>
    // Stack Overflow
   var chat = document.getElementById("chat");
chat.scrollTop = chat.scrollHeight;

</script>
</html>